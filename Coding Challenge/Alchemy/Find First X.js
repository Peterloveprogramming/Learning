Your Goal: Find the First X
In the string argument find the index of the first lower-case "x" and return it.
--

function findFirstX(string) {
    let b = string.indexOf("x")
    return b
}

module.exports = findFirstX;

