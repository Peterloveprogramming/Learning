Check if a value is classified as a boolean primitive. Return true or false.

Boolean primitives are true and false.

---
  
  function booWho(bool) {
  let c = 3>2;
  if (typeof(bool)=== typeof(c)){
      return true
  }
  return false
}
booWho(null);
